management:
  endpoints:
    web:
      exposure:
        include: health, info #"*"
  endpoint:
    health:
      show-details: always

# health 체크 대상에서 빼고 싶은 요소들
management.health.redis.enabled: false
