# 커밋메시지 작성 절차

이 문서는 이 저장소의 커밋 메시지 작성 기준만 정의한다.

- 커밋 메시지는 한글로 작성한다.
- 제목은 `~함` 형태로 끝낸다. 예: `보안 인증 흐름 정리함`
- 단순 변경은 제목만 사용한다.
- 여러 책임이 함께 바뀐 경우에만 본문을 짧게 쓴다.
- 본문에는 주요 변경점과 검증 결과만 적는다.
- 실행하지 않은 검증을 실행한 것처럼 쓰지 않는다.
- 민감값, 개인 경로, 임시 파일, 생성 파일은 커밋 메시지에 포함하지 않는다.
