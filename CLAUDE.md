# CLAUDE.md

이 파일은 AI 에이전트 호환용 진입점이다.

실제 공통 작업 지침은 루트 `AI.md`와 `.AI/README.md`를 기준으로 한다.
Claude를 포함한 어떤 Agentic Coding 도구를 사용하더라도 루트 `AI.md`를 먼저 읽고 따른다.

Claude command나 rule을 만들 때도 공통 절차는 `.AI/procedures/**`에 두고, `.claude/**`에는 얇은 adapter만 둔다.
문서 간 내용이 충돌하면 루트 `AI.md`와 `.AI/policies/**`를 우선한다.
